node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# deps / build
node_modules/
dist/
coverage/
*.log

# env
.env
.env.*

# OS
.DS_Store

# local DB artifacts (keep schema + migrations, ignore runtime files)
src/db/drizzle/db.sqlite
src/db/drizzle/meta/

 # uploads are runtime (ignore files, keep folders)
uploads/**
!uploads/
!uploads/gallery/
!uploads/services/
!uploads/.gitkeep
!uploads/gallery/.gitkeep
!uploads/services/.gitkeep

# local sqlite/db artifacts (never commit)
barber.db
barber.sqlite
*.db
*.sqlite
*.sqlite3
